export const fullName = stringField("fullName", 4, 30, /^[a-zA-Z ]{4,30}$/); export const password = stringField("password", 8, 50, /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[^a-zA-Z0-9]).{8,50}$/); export const emailField = z.string().email("Invalid email format"); export const phoneField = stringField("phone", 7, 20, /^\+?[0-9\s\-().]{7,20}$/); export const phoneTwoField = stringField("phone Two", 7, 20, /^\+?[0-9\s\-().]{7,20}$/); export const genderField = z.nativeEnum(Gender); export const nationalityField = stringField("nationality", 2, 60, /^[A-Za-z\s-]{2,60}$/); export const linkedInUsername = stringField("linkedInUsername", 5, 40, /^[a-zA-Z0-9-]{5,40}$/) export const portfolioUrl = stringField("portfolioUrl", 9, 40, /^https?:\/\/[^\s]{9,38}\.[^\s]+$/) export const roleField = enumField("role",[Role.Admin,Role.SuperAdmin,Role.SystemAdmin,Role.User]); export const limitedRoleField = enumField("role",[Role.Admin,Role.SuperAdmin,Role.User]); export const otpField = z.string().length(6, "OTP must be exactly 6 digits"); export const verificationTokenField = z.string(); export const addressLine1Field = stringField("addressLine1", 3, 100); export const addressLine2Field = stringField("addressLine2", 0, 100); export const cityField = stringField("city", 2, 50, /^[A-Za-z\s]{2,50}$/); export const stateField = stringField("state", 2, 50); export const districtField = stringField("district", 2, 50); export const countryField = stringField("country", 2, 60, /^[A-Za-z\s]{2,60}$/); export const postalCodeField = stringField("postalCode", 3, 10, /^[0-9]{3,10}$/); export const landmarkField = stringField("landmark", 4, 100); export const primary = booleanField("primary"); export const jobTypeEnum = z.nativeEnum(JobType); export const workModeEnum = z.nativeEnum(WorkMode); export const currentSalaryField = numberField("currentSalary", 0, 100000000); export const expectedSalaryField = numberField("expectedSalary", 0, 100000000); export const immediateJoinerField = z.coerce.boolean(); export const experienceField = stringField("experience", 1, 100); export const currentDesignationField = stringField("currentDesignation", 2, 100); export const currentCompanyField = stringField("currentCompany", 2, 100); export const currentJobTypeField = jobTypeEnum; export const preferredJobTypesField = jsonArrayParser(jobTypeEnum); export const preferredWorkModesField = jsonArrayParser(workModeEnum); export const noticePeriodField = z .coerce.number() .or(z.nan()) .refine( (val) => val == null || val >= 0, "Notice period must be positive" ); export const companyNameField = stringField("companyName", 2, 100, /^[A-Za-z0-9\s.&-]{2,100}$/); export const designationField = stringField("designation", 2, 100, /^[A-Za-z0-9\s.&-]{2,100}$/); export const industryField = stringField("industry", 2, 100, /^[A-Za-z\s]{2,100}$/); export const jobDescriptionField = stringField("jobDescription", 10, 5000, /^.{10,5000}$/s); export const benifitsField = stringField("benifits", 2, 1000, /^.{2,1000}$/s); export const salaryField = numberField("salary", 1, 1000); export const skillsField = stringField("skills", 2, 500, /^.{2,500}$/s); export const vacancyField = numberField("vacancy", 1, 1000); export const packageNameField = stringField("packageName", 2, 100, /^[A-Za-z0-9\s.&-]{2,100}$/); export const descriptionField = stringField("description", 10, 1000, /^.{10,1000}$/s); export const priceINField = stringField("priceIN", 1, 20, /^\d+(\.\d{1,2})?$/); export const priceUAEField = stringField("priceUAE", 1, 20, /^\d+(\.\d{1,2})?$/); export const packageTypeField = enumField("packageTypeField",[PackageType.jobPackage, PackageType.tourPackage]); export const packageDurationField = numberField("packageDuration", 1, 365); export const imageField = stringField("Image", 3, 500, /^[A-Za-z0-9 ._\-\/+=!@#$%^&*(){}\[\]|:;,?~]{3,500}$/); export const featuresField = stringArrayField("features", 1, 10, 1, 200, /^[A-Za-z0-9 .&-]{1,200}$/); export const foodField = booleanField("food"); export const accommodationField = booleanField("accommodation"); export const travelCardField = booleanField("travelCard"); export const utilityBillsField = booleanField("utilityBills"); export const airportPickupField = booleanField("airportPickup"); export const jobGuidanceField = booleanField("jobGuidance"); export const customerIdField = stringField("customerId", 1, 100, /^[A-Za-z0-9-_]{1,100}$/); export const packageIdField = stringField("packageId", 1, 100, /^[A-Za-z0-9-_]{1,100}$/); export const customerNameField = stringField("customerName", 2, 100, /^[A-Za-z0-9\s.&-]{2,100}$/); export const totalAmountField = numberField("totalAmount", 0, 100000000); export const paidAmountField = numberField("paidAmount", 0, 100000000); export const paymentMethodField = enumField("paymentMethodField",[PaymentMethod.bankTransfer, PaymentMethod.cash, PaymentMethod.googlePay]); export const paymentstatusField = enumField("paymentstatusField",[PaymentStatus.fullyPaid, PaymentStatus.partiallyPaid, PaymentStatus.pending]); export const paymentDateField = stringField("paymentDate", 1, 40, /^.{1,40}$/); export const referenceIdField = stringField("referenceId", 3, 50, /^[A-Za-z0-9-_./]{3,50}$/); export const paymentProofField = stringField("paymentProof",1,500,/^[A-Za-z0-9 ._\-\/+=!@#$%^&*(){}\[\]|:;,?~]{1,500}$/); export const adminNotesField = stringField("adminNotes", 0, 500, /^.{0,500}$/s); export const s3FileKey = stringField("s3FileKey", 6, 500, /^(resumes|profiles|packages|payments)\/[A-Za-z0-9._\-]+$/); in this remove duplicate and correct spelling and also remove "Field" from the constants and also make the regex to another constnat woth meaningfull names for reusablity and use that regext reusable constant in these lines